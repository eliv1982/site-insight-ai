# Canonical OpenAI-compatible provider settings.
OPENAI_API_KEY=replace-with-provider-api-key
BASE_URL=https://llm-provider.example/v1

# Compatibility aliases. Use these only when the canonical variable is absent.
# API_KEY=replace-with-provider-api-key
# PROXY_API_URL=https://llm-provider.example/v1
